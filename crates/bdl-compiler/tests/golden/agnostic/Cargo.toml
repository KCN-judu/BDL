# Generated by bdl-codegen-rust 0.1.0. Do not edit.
[package]
name = "bdl_design_agnostic"
version = "0.1.0"
edition = "2021"
publish = false

# A generated crate is its own workspace, wherever it is written.
[workspace]

[lib]
path = "src/lib.rs"

# Named after the package so several generated crates can share one
# target directory without their host binaries overwriting each other.
[[bin]]
name = "bdl_design_agnostic-host"
path = "src/bin/host.rs"
required-features = ["host"]

[features]
default = []
# The std host bridge; the core itself is no_std.
host = ["dep:bdl-runtime-host"]

[dependencies]
bdl-runtime-core = { path = "../../runtime/bdl-runtime-core" }
bdl-runtime-host = { path = "../../runtime/bdl-runtime-host", optional = true }
