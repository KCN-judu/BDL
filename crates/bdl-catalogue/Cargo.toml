[package]
name = "bdl-catalogue"
description = "The catalogue of realization profiles a deployment may assign: output profiles (encoder + requirements) and input profiles (transducer + requirements), each with an origin the semantics never reads; builtin today, package-supplied later"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bdl-model = { workspace = true }
bdl-ir = { workspace = true }
serde = { workspace = true }

[lints]
workspace = true
