[package]
name = "bdl-runtime-core"
description = "no_std runtime vocabulary shared by every generated BDL semantic core: active domains, checked numerics, structured runtime errors"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[features]
default = []
# Finite collections (`list τ`): `alloc::vec::Vec` in the generated core.
# Enabled by the generator exactly when the design carries list values; a
# target then needs a global allocator.  Everything else stays
# allocation-free.
collections = []

[lints]
workspace = true
