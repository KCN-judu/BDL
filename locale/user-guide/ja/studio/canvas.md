<!-- scripts/docs_l10n.py が docs/user-guide/studio/canvas.md から生成しました。locale/user-guide/ja/user-guide.po を編集してください。このファイルは編集しないでください。 -->

> 言語: [English](../../../../docs/user-guide/studio/canvas.md) · [简体中文](../../zh_Hans/studio/canvas.md) · 日本語

# キャンバス

キャンバスは設計ページの中央で、設計の構造の絵です。その上のすべてに意味があり、各視覚チャネルはただ 1 つのことを意味します。

## 印の意味

| 見えるもの | 意味 |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **ソケットの色** | _どのコンセプトか_——各コンセプトは固有の色相を持ち、現れる場所すべてで同じ |
| **ソケットの形** | コンセプトの _値の形式_：○ 量、◇ オン/オフ、□ 個数。値の形式が _後で決める_ の間は**中空の輪** |
| **概念の色をした、ソケットからソケットへのリンク** | シグネチャ：_この関係はその概念を読み取る_（概念 → 関係の入力）、_この関係はその概念を生成する_（関係 → 概念）、または _この値はその出力を駆動する_（関係 → シンク） |
| **数式行で終わる細い灰色のリンク** | 参照：_この数式はその関係を名指している_——値がルールを適用している、あるいは値や入力元を名指している。どちらの端も引っ張れず、数式はインスペクターで編集する |
| **破線の輪郭** | _宣言済み_：何かを読み取るが数式のない関係。駆動元のない、またはドメインのない出力 |
| **ノード左端の縦バー、入る矢印、_入力元_ の語** | **入力元**：環境から与えられる値——何も読み取らず数式を持たない関係。欠けているものは何もない |
| **数式行の赤い印** | 数式が検査を通らない——_今、間違っている_ のであって、_まだ_ ではない |
| **_宣言済み_、_競合_、_不正_、_ドメインなし_ の語** | ノードが持ちうる唯一の状態語。その状態が成り立つ間だけ（駆動されなければならない出力はその間 _必須_ と表示され、入力元は _入力元_ と表示される） |
| **語 _ルール_** | 何かを読み取る関係——関数であり、値の数式がこれを適用するまで固有の値を持たない。状態語がその位置を使っていないときに表示される |
| **ノード右端の小さな名前** | そのタイミングドメイン |
| **アクセントカラー** | 選択——それ以外にはない |

位置、大きさ、リンクの向きには**意味はありません**。データは読みやすさのために左から右へ描かれます。色付きのリンクはシグネチャを、灰色のリンクは何が何に依存するかを示します。どちらも実行順序を示さず、演算子ごとのノードもありません——算術は数式フィールドにあります。

## ノード

![Concept rows Tilt and Brightness with a round socket at each end; relationship nodes with a name header, one input socket per concept read on the left, one output socket on the right and the formula in the body; dimByTilt, with the word rule in its header, outlined in the accent colour because it is selected; the Source tilt with a bar at its left edge, an entry arrow and the word Source in its header and no input socket; brightness with no input socket and two thin grey links arriving at its formula line from the output sockets of tilt and dimByTilt; the light sink at the right with the word required in its header, a single input socket and a bar at its right edge; tilt, brightness and the output carry the domain name interaction at their right edge.](../../../../docs/user-guide/assets/studio/node-anatomy.png)

_傾きランプのノードの構造：コンセプト行、関係ノード（dimByTilt を選択）、light シンク。_

**コンセプト**は 1 行です。名前、左の入力ソケット（何かがこのコンセプトを生成する）、右の出力ソケット（関係はここから読み取る）。両方のソケットがコンセプトの色と形を持ちます。

**関係**は箱です。ヘッダーには名前と、該当する間だけ唯一の状態語（何かを読み取り数式がまだない関係には _宣言済み_）。左側には読み取る概念ごとに一つの入力ソケットがあり、それぞれ概念名が添えられます。右側には生成する概念名を添えた出力ソケットが一つ。その下の行に数式があり、関係にタイミングドメインがあれば右端にその名前が出ます。

箱を見れば、数式を読まずに三つの形を見分けられます。何かを読み取る関係——上の `dimByTilt`——は**ルール**です。入力ソケットがあり、ヘッダーの語を他に必要とするものがなければ _ルール_ と表示されます。関数なので、値の数式がこれを適用するまで設計は何も計算しません。何も読み取らず数式を持つ関係——`brightness`——は**値**です。入力ソケットも語もなく、その数式が依存する値とルールを名指します。名指された関係のそれぞれは**参照リンク**で結ばれます。その関係の出力ソケットから `brightness` の数式行の左端へ引かれた細い灰色の線です。参照リンクは設計の依存関係で、コンパイラーによる数式の分析から読み取られます。分析が届いたときに現れ、引っ張ることはできません——変えるには数式を変えます。どの数式にも適用されないルールからは、どの数式行へも参照リンクは出ていきません。

**入力元**は、何も読み取らず数式を持たない関係です——上の `tilt`。その値は環境から、アクティベーションごとに一度与えられます。ヘッダーに _入力元_ の語、名前の前に入る矢印、**左**端に縦のバー（環境側です。設計の中の何もそれに値を送りません）、出力ソケット 1 つ、入力ソケットなしで描かれます。破線でも _宣言済み_ でもありません。欠けているものは何もないのです。実際の製品で何がその値を与えるか——センサー、ボタン、アナログ線——は配備の事柄であって、キャンバス上の印ではありません。入力元に数式を与えれば、設計の中で計算される通常の関係になります。

**物理出力**はシンクです。名前と右側の語を持つ色付きのヘッダー——設計が完成する前に駆動しなければならない出力には _必須_、あるいはそれを上書きする状態（_ドメインなし_、_競合_、_不正_）。受け取るコンセプト名付きの入力ソケット 1 つ。右端にタイミングドメイン。右側に縦のバー——そこから何も流れ出ません。

選択中のノード（上の `dimByTilt`）はアクセントカラーで縁取られます。キャンバス上の他の何もその色を使いません。

コンポーネントを組み合わせるプロジェクトには、**インスタンスノード**（ポートごとに 1 行、本体にコンポーネント名）と、**振る舞い領域**または折りたたまれた**振る舞いボックス**も表示されます。[システムプロジェクト](../../../../docs/user-guide/studio/system-projects.md) を参照。

すべてのノードに場所があります。あなたが配置していないノード——コードビューやコードエディタで作られたもの、古いプロジェクトの初回オープンで生じたもの——は自動で配置されます。その種類の列（左にコンセプト、中央に関係、右に出力）で、読み取るもの・生成するものの横、すでにあるものの下に。あなたが配置したものは動きません。好きな場所にドラッグしてください（[設計・コード・分割](code-view.md)）。

## ジェスチャ

| 操作 | 結果 |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 空のキャンバスをドラッグ | パン |
| スクロールホイール / ピンチ | ポインタを中心にズーム |
| Home または ⌘0 | 設計全体を表示 |
| ノードをクリック | 選択する（インスペクターが追従） |
| ⇧-クリック | 複数選択に加える / から外す |
| 空のキャンバスで ⇧-ドラッグ | 矩形選択 |
| ノード本体をドラッグ | 移動する。離すと位置が保存される（設計の変更ではない） |
| 出力ソケットから入力ソケットへドラッグ | リンクを作る。ドラッグ中、受け入れられるすべてのソケットにハローが、互換性のないソケットには禁止カーソルが表示される |
| 接続済みの入力ソケットから引き離し、空のキャンバスで離す | 切断 |
| ドラッグ中のリンクを空のキャンバスに落とす | 何も起きない（ノードは作られない） |
| ⌫ / Delete | 選択を削除する。使用中のコンセプトは使用者を挙げたバナーとともに拒否される |
| コンセプトまたは関係ノードをダブルクリック | その場で名前を変更する（出力はインスペクターで名前を変更。インスタンスはソースを開く） |
| 右クリック | コンテキストメニュー（下記） |
| ライブラリタブの行をキャンバスへドラッグ | そのコンセプトを落とした位置に挿入する。名前が編集状態で開く |

<!-- figure F7: a link in mid-drag with the halo — pending, see SCREENSHOT_PLAN.md -->

## コンテキストメニュー

空のキャンバスで：**コンセプトを追加 ▸**——_最近_、_入力_、_出力_、最もよく使う 3 つのカテゴリ、_その他…_（ライブラリタブを開く）。**入力元を追加 ▸**——標準の入力元（_温度センサー_、_傾きセンサー_、…）と、設計にすでにあるコンセプトの上に入力元を作る _新しい入力元…_。**インスタンスを追加 ▸** _コンポーネント_ と**新規振る舞いグループ**。

ノード上で：**名前を変更**、**削除**。関係ではさらに**振る舞いとしてグループ化**、**グループに追加 ▸**、**… から外す**。インスタンスでは**ソースを編集**。振る舞いでは**折りたたむ** / **展開**、**再利用可能なコンポーネントとしてパッケージ化…**、**グループ解除**。

## キャンバスが決して示さないもの

下書きの数式（入力しただけで追加していない数式はキャンバス上の何も変えません——参照リンクも変わりません。ステータス行が数えます）、値（シミュレートページにあります）、関係が自身の前回の値を覚えていること（`delay` はリンクを描きません）、識別子や型名（説明にあります）。

## 関連

[ワークスペース](workspace.md) · [インスペクター](inspector.md) · [システムプロジェクト](../../../../docs/user-guide/studio/system-projects.md) · [キーボードとマウス](../reference/keyboard-and-mouse.md)
